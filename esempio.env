# Cerca Dai Testi - Configurazione Ambiente
# Copia questo file in .env e inserisci i tuoi valori

# === API KEYS ===
# Token per Genius API (obbligatorio)
# Ottienilo da: https://genius.com/api-clients
GENIUS_API_TOKEN=your_genius_api_token_here

# === CACHE ===
# Directory per la cache (default: .cache)
CACHE_DIR=.cache

# Ore prima che la cache scada (default: 24)
CACHE_EXPIRY_HOURS=24

# === RATE LIMITING ===
# Numero massimo di chiamate API per periodo
RATE_LIMIT_CALLS=10

# Periodo in secondi per il rate limiting
RATE_LIMIT_PERIOD=60

# === NLP ===
# Modello sentence-transformers da usare
# Default: paraphrase-multilingual-MiniLM-L12-v2 (supporta italiano e altre lingue)
# Alternative: all-MiniLM-L6-v2 (solo inglese, più veloce)
EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# === LOGGING ===
# Livello di log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# File per salvare i log (opzionale, lascia vuoto per solo console)
LOG_FILE=
